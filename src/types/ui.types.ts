export interface IControlProps {
  name: string;
  type: string;
  label?: string;
  placeholder?: string;
  children?: any;
}
export interface IRangeProps {
  nameFrom: string;
  nameTo: string;
  label?: string;
  labelTo?: string;
  labelFrom?: string;
}
